{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -n \"$DIRECT_URL\" ]; then DATABASE_URL=$DIRECT_URL npx prisma db push --skip-generate --accept-data-loss; fi && npm run build && mkdir -p .next/standalone/.next && cp -r .next/static .next/standalone/.next/ && cp -r public .next/standalone/"
  },
  "deploy": {
    "numReplicas": 1,
    "startCommand": "HOSTNAME=0.0.0.0 PORT=${PORT:-8080} node .next/standalone/server.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
