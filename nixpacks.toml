# Nixpacks configuration for Railway

[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "mkdir -p .next/standalone/.next",
  "cp -r .next/static .next/standalone/.next/",
  "cp -r public .next/standalone/",
  "cp -r prisma .next/standalone/"
]

[start]
cmd = "npx prisma db push --accept-data-loss --skip-generate || echo 'DB push failed'; HOSTNAME=0.0.0.0 PORT=${PORT:-8080} node .next/standalone/server.js"
